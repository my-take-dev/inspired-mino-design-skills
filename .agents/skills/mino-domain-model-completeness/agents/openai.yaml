interface:
  display_name: "Domain Model Completeness"
  short_description: "必要な概念・状態・制約・失敗と全writer・readerの欠落を監査する"
  default_prompt: "$mino-domain-model-completenessで、依頼範囲の判断・根拠・未決事項と検証結果を返す。"
policy:
  allow_implicit_invocation: true
