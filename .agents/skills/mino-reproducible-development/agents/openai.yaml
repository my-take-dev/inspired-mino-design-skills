interface:
  display_name: "Reproducible Development Router"
  short_description: "必要な専門成果物を統合し、実装・独立確認・要件再現性まで追跡する"
  default_prompt: "$mino-reproducible-developmentで、依頼範囲の判断・根拠・未決事項と検証結果を返す。"
policy:
  allow_implicit_invocation: true
