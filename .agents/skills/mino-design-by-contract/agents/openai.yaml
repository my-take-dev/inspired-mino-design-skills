interface:
  display_name: "Design by Contract"
  short_description: "条件ごとの保証責任・失敗後状態・適用判定・観測可能なテストを設計する"
  default_prompt: "$mino-design-by-contractで、依頼範囲の判断・根拠・未決事項と検証結果を返す。"
policy:
  allow_implicit_invocation: true
