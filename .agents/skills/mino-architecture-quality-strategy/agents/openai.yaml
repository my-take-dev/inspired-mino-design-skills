interface:
  display_name: "Architecture Quality Strategy"
  short_description: "事業価値と品質の優先順位から全体構造・正本・移行・復旧を設計する"
  default_prompt: "$mino-architecture-quality-strategyで、依頼範囲の判断・根拠・未決事項と検証結果を返す。"
policy:
  allow_implicit_invocation: true
