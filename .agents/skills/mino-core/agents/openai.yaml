interface:
  display_name: "Shared Core"
  short_description: "七つの設計スキルの根拠・権限・要件追跡・共通実行規則を統一する"
  default_prompt: "$mino-coreで、callerが必要とする共通recordと不足だけを返す。"
policy:
  allow_implicit_invocation: false
