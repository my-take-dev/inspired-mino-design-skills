# Evaluation 0.6.0

Evaluation date: 2026-07-14

Evaluation scope: solver routing metadata isolation、fail-safe benchmark metric、platform traceability、validator parity、logical runtime path、debt priority、context relationship failure semantics、term referential integrity、macOS release scope。

solverにはoracleを含まない隔離済み`skills/` copyとraw caseだけを渡す。ここにはrun数、metric、原因component、未実行事項、残存riskだけを残す。

## Structural checks

| Check | Result |
|---|---|
| Linux Bash validator | pass。7 Skill / 30 Markdown、Errors 0、Warnings 0 |
| Windows PowerShell 5.1 validator | pass。WSL UNC上の同一artifactで7 Skill / 30 Markdown、Errors 0、Warnings 0 |
| malformed manifest empty-key fixture | pass。空keyと空白keyを両validatorがexit 1、Errors 2、Warnings 0で拒否 |
| solver runner / routing metadata reinsertion | pass。top-level `case_id`を両validatorがexit 1、Errors 1、Warnings 0で拒否 |
| exact required heading probe | pass。末尾空白付きheadingを両validatorがexit 1、Errors 1、Warnings 0で拒否 |
| exact Contents probe | pass。末尾空白付きContentsを両validatorがexit 0、Errors 0、Warnings 1で報告 |
| Skill package quick validation | pass。7 Skillすべてvalid |
| repository text format | pass。suite validatorとREADMEのBOM / CR / final newline検査 |
| `git diff --check` | pass |

## Run summary

| Task | Runs | Mode / isolation | Result |
|---|---:|---|---|
| C1〜C4 regression | 0 | not executed | inherited 0.5.0 Evidenceを0.6.0 passとして流用しない |
| C5 draft iteration 1 | 3 | design / oracleなしのisolated suite / fresh context | 0 / 3 strict pass。意味分離とunknown隔離は保ったが、translation / relationshipのcanonical fieldとEvidenceを圧縮した |
| C5 draft iteration 2 | 3 | design / oracleなしのisolated suite / fresh context | 1 / 3 strict pass。3 runともcanonical IDとreference integrityは改善。2 runはterm actor / purpose / rule / Evidence、unknown解決field、またはpriority owner Evidenceを省略した |
| C5 final field upgrade | 0 completed | design / isolated smoke | iteration 2のgapを`confirmation_method`、`impact_if_unresolved`、term / priority hard gateへ反映した後のrun artifactは未取得。passに数えない |

## Unexecuted validation

- C1〜C4と、最終field upgrade後C5の固定promptによる最低3 fresh-context strict run。
- B1〜B9、N1〜N7の全matrixと、前versionでpassした全taskの回帰。
- native Windows checkout / NTFS上のvalidatorとmalformed fixture probe。
- 実repositoryのimplementation、contract test、failure injection、migration rehearsal、外部provider sandbox。
- application-levelのWindows / Linux同一oracle。

## Residual risks

- C5 draft iteration 2は1 / 3 strict passで、solverがcanonical schemaを安定して返すことをまだ証明しない。
- 最終field upgradeは構造検証だけであり、fresh-context behavioral Evidenceがない。
- Windows PowerShell validatorはWindows processで実行したが、artifactはWSL UNC上にあり、native Windows checkout / NTFS Evidenceではない。
- sampled caseと単一model / settingに偏り、application runtime correctnessを証明しない。
- full release gateを満たしていない。

Evaluation status: structural and targeted verification recorded; targeted behavioral Evidence partial; suite release not ready.
