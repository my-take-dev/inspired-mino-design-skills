# Evaluation 0.7.0

Evaluation date: 2026-07-14

Evaluation scope: unknown / selection fail-safe、standalone package completeness、router mode artifact、solver payload isolation / provenance、manifest / metadata / path / text validator parity。

## Contents

- Contract changes under evaluation
- Structural and fixture checks
- Behavioral run accounting
- Unexecuted validation
- Residual risks
- Evaluation status

## Contract changes under evaluation

- Core: structured unknown / unexecuted validation、Selection Gate、causal applicabilityのunknown、new-capability causal Evidence。
- Requirements: edge単位のtrace rationale / validation / Evidenceとstructured N/A。
- Architecture: unknownを含むpriority / reversibility / approval / recovery、Selection Gate、temporary path、Platform Package。
- Completeness: canonical Domain Discovery Package、全gap dimension、transition control、Platform Package。
- Contract: retry / duplicate obligation、公開契約migration、Change Safety、Platform Package。
- Boundary: operation semanticsごとのunknown、duplicate独立判定、stable trace ID、Change Safety、Platform Package。
- Router: 4 mode artifact、function plan結果、causal chain / Selection Gate、Change Safety、詳細な未実行validation。
- Benchmark: exact fence payload、explicit / implicit selection境界、evaluation workspace隔離、run provenance、fail-safe metric status。
- Validators: manifest grammar / uniqueness / containment、version coherence、case allowlist、front matter / metadata exact structure、runtime path、Contents、case-insensitive text inventory。

`skills/mino-core/evaluations/fixtures/0.7.0/`のinvalid manifest値はvalidatorへ拒否させるnegative inputであり、runtime参照またはpath解決入力として利用しない。

## Structural and fixture checks

| Check | Result |
|---|---|
| Linux Bash validator | pass。7 Skill / 33 Markdown、Errors 0、Warnings 0 |
| Windows PowerShell 5.1 validator | pass。WSL UNC上の同一artifactで7 Skill / 33 Markdown、Errors 0、Warnings 0 |
| Linux validator fixture runner | pass。positive control、3 CLI exit parity、20 negative / parity probeの計24件すべて期待exit / message |
| Windows PowerShell validator fixture runner | pass。positive control、3 CLI exit parity、20 negative / parity probeの計24件すべて期待exit / message |
| logical path and Skill reference resolution | pass。両validatorのcurrent suite validationに含む |
| repository UTF-8 / BOM / CR / final newline | pass。tracked-or-new text 89件、error 0 |
| exact solver payload digest | pass。8 fence bodyのSHA-256がrunner-only metadataと一致 |
| `git diff --check` | pass |

構造validatorとnegative fixtureは、Skillが期待する判断を返すbehavioral Evidenceではない。fixture runnerはvalidatorのfail-open回帰だけを検査する。

## Behavioral run accounting

| Case set | Counted runs | Result |
|---|---:|---|
| C1〜C8 on 0.7.0 | 0 | not executed |
| B1〜B9 / N1〜N7 full release matrix | 0 | not executed |
| 0.6.0 summaries considered for inheritance | 0 | not inherited |

0.6.0の記録にはrun ID、model / setting、runtime suite digest、exact input / output digest、workspace exclusionの完全なprovenanceがない。観測を捏造または削除はしないが、0.7.0のcounted runや回帰passへ継承しない。

## Unexecuted validation

- C1〜C8を、oracleとevaluation directoryを除外したfresh contextで同一model / settingにより各最低3 runすること。
- B1〜B9、N1〜N7、過去pass taskの全Release gate回帰。
- native Windows checkout / NTFS上のvalidator、fixture runner、text / case sensitivity挙動。
- 対象applicationのcontract test、failure injection、migration rehearsal、external provider sandbox。
- application levelのWindows / Linux同一oracle検証。

必要runnerはfresh-context evaluator、isolated solver workspace、model setting recorder、SHA-256 recorderである。ownerはsuite maintainerが割り当てるまでunknownであり、未実行理由とcommandを一つのfree textへ結合しない。

## Residual risks

- canonical schemaを増強したが、solverがC1〜C8でfieldをlosslessに安定生成するbehavioral Evidenceはまだない。
- strict validatorは構文・package整合を検査するが、Markdown内の業務主張や対象application runtime correctnessを証明しない。
- PowerShellの実行が成功してもWSL UNC上ならnative Windows checkout / NTFS Evidenceではない。
- 0.7.0は0.6.0のsummaryを回帰passへ継承しないため、behavioral release判定に必要なrun母数は0である。

## Evaluation status

Structural verification: pass for Linux and Windows PowerShell over the same WSL UNC artifact; native Windows checkout / NTFS remains unexecuted.

Targeted behavioral Evidence: not executed.

Behavioral release: not ready.
