# Evaluation 0.4.0

Evaluation date: 2026-07-11

Evaluation scope: Windows / Linux platform contract、各Functionのplatform固有gate、両OS用suite validator。

## Structural checks

| Check | Result |
|---|---|
| Linux: `skills/mino-core/scripts/validate-suite.sh` with Bash 5.3 on WSL2 | pass: 6 skills、22 markdown files、0 errors、0 warnings |
| Windows: `skills/mino-core/scripts/validate-suite.ps1` with Windows PowerShell 5.1 | pass: 6 skills、22 markdown files、0 errors、0 warnings |
| Bash syntax validation | pass |
| validator parity | pass: skill set、frontmatter、metadata、logical path、reference、line-count rules、platform routingを両scriptで検査 |
| all Function routing | pass: 6 / 6 skillsが`skills/mino-core/references/platform-compatibility.md`を直接routing |
| validator packaging | pass: Windows用PowerShellとLinux用Bashを同じ`skills/` rootへ同梱 |
| isolated relocation with only `skills/` under a path containing spaces | pass: Linux Bash、Windows PowerShellとも6 skills、22 markdown files、0 errors、0 warnings |

Windows検証はWSL2からWindows PowerShell 5.1を起動し、WSL上のsuiteをUNC path経由で読み取って実行した。PowerShell runtimeとWindows path handlingのEvidenceではあるが、native Windows checkout上のNTFS挙動を網羅するものではない。

## Contract checks

| Skill | Windows / Linux adaptation |
|---|---|
| mino-core | Platform Context、共通execution rule、platform別validation schema、両validator routing |
| mino-architecture-quality-strategy | deployment platform、品質scenario、operation / recovery parity |
| mino-domain-model-completeness | platform固有writer / reader / representation / failureの監査 |
| mino-design-by-contract | environment condition、共通contract oracle、platform別test result |
| mino-interface-implementation-separation | OS固有path / process / shellをimplementation adapterへ隔離 |
| mino-reproducible-development | required platformごとの独立検証とparity decisionの統合 |

## Behavioral validation

- B9とN7をbenchmarkへ追加し、未検証platformを両OS対応済みと宣言しないrelease gateを定義した。
- suite構造validatorはWindows / Linuxの両runtimeでpassした。
- sample applicationを使うWindows / Linux同一contract test、filesystem failure、process cancellationのmulti-run reproduction testは未実行である。

## Unexecuted validation

- native Windows checkout / NTFS上でのcase、ACL、file lock、junction、CRLF挙動。
- Linux native filesystem上でのpermission、executable bit、symlink、signal、atomic replaceのapplication-level scenario。
- B9とN7の各3 fresh-context run。
- Windows / Linux CI runnerを使った継続的なvalidator parity。
- Bash 4およびPowerShell 7での追加version matrix。

## Residual risks

- 両validatorの検査契約は手動で対応付けており、自動的な同値性証明ではない。
- WSL / UNC経由のWindows検証は、native Windows repositoryのfilesystem semanticsを完全には再現しない。
- platform compatibility referenceは未知のtoolchain固有差を一般化しないため、対象repositoryごとのtest commandとadapterが別途必要である。
- behavioral release gateを満たしていないため、suite全体のWindows / Linux runtime parityは`incomplete`である。

Evaluation status: Windows / Linux structural validators pass; application-level cross-platform reproduction remains incomplete.
