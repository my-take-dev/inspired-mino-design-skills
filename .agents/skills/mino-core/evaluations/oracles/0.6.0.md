# Evaluator oracles 0.6.0

solverへこのfileを渡さない。runner metadataをpromptやworkspaceへ混ぜず、run完了後に固定gateとして採点する。

## Contents

- Runner-only metadata
- Suite contract oracle
- C1 oracle
- C2 oracle
- C3 oracle
- C4 oracle
- C5 oracle

## Runner-only metadata

| Case | Invocation | Selection under evaluation |
|---|---|---|
| C1 | `$mino-reproducible-development` | explicit integrated entry |
| C2 | `$mino-architecture-quality-strategy` | explicit Architecture entry |
| C3 | `$mino-interface-implementation-separation` | explicit Boundary review entry |
| C4 | implicit selection from the installed suite | public Problem Framing routing |
| C5 | `$mino-reproducible-development` | explicit integrated entry |

runnerはInvocationをsolver inputへfieldとして埋め込まない。C4では期待Skill名をsolverへ渡さない。

## Suite contract oracle

- solver caseのtop-level fieldにrunner用`case_id`、`skills`、期待routingを含めない
- benchmarkのboolean metricは未検証時にすべて`false`で始まり、`interface_leakage`をfail-openにしない
- platform resultはrequirement、contract、verification、oracleのIDを保持し、全required platformの集合一致を確認するまでparityをpassにしない
- malformed manifestの空keyをWindows / Linux validatorが同じinvalid line contractと非zero exitで拒否する
- required heading、Hard Gates、100行超referenceのContentsは末尾空白を許容せず両validatorで完全一致にする
- Skill内部のcanonical decision参照は`skills/`から始まる論理pathへ解決する

## C1 oracle

Required:

- actor、problem、unknown recovery policy、candidate meansを分け、AI restatementを残す
- Architectureはauthority / recovery slice、Completeness、Contract、Boundaryを`run_if`付きでroutingする
- quantity、money、state、transition、failure、time、全writer / reader / authorityをrequirement別matrixへ保持する
- destruction probeがwriterからpartial failure、状態・副作用、顧客 / 運用impactまで追う
- condition単位contract ID、authority type / owner、defensive validation、test ID / oracleを接続する
- idempotencyはconfirmでrequired、値objectのpure operationへkeyを作らない
- transport backoff、operation retry可否、ambiguous outcome recovery ownerを分ける
- recovery policyはunknownのままoptionとEvidence取得方法を示す
- problem readinessはconditionalとして未決事項を選択gateへ隔離し、選択・実装だけをblockする
- design artifactのready / plannedと、後続implementation / releaseのblockerを分ける

Forbidden:

- genericな概念listだけでcoverageを主張する
- operation全体のowner一つへ全条件を置く
- provider追加用Strategy / factory
- planをpassed / executedとする
- unknown recovery policyをconfirmedにする

## C2 oracle

Required:

- domain visionを`not_applicable`とし、value-preservation / risk statementを使う
- security、recoverability、modifiability等をportfolioと観測可能scenarioへ変換する
- do nothing / do minimum / incrementalを含むoptionとtrade-offを比較する
- value → scenario → current finding → option → target decision → transition phase → validationをIDでtraceする
- target source of truth / authorityを未決として扱い、選択gateを示す
- transitionへowner、deploy order、exit、irreversible point、abort、recovery、old path removalを持たせる
- Windows / Linux recordが同じrequirement ID、contract ID、verification ID、oracle refを保持し、未実行結果とparity incompleteを分ける
- design artifact readyとruntime validation未実行を分ける

Forbidden:

- unique value、ROI、計測値の捏造
- managed object storageの先行採用
- trace IDなしのplatform parity判定
- transitionなしのtargetだけの提案
- migration / cutoverの実行

## C3 oracle

Required:

- Skillの非適用または`keep-branch / keep-concrete`を選ぶ
- guardのpurpose、固定contract、既存testを根拠にする
- add / change variant scenarioを`not_applicable`とし理由を示す
- review modeを守り、必要なら現行test実行だけを提案する

Forbidden:

- interface、factory、Strategy、追加layer
- full integrated workflowまたはArchitectureへのrouting
- 将来変更を根拠なく仮定する

## C4 oracle

Required:

- public entryとして`$mino-problem-framing`を選び、統合routerや専門Functionを過剰起動しない
- Redisを未選択のcandidate meansへ退避し、購入者、観測遅延、成功条件、公開契約維持をProblem Frameへ置く
- bottleneckと原因をunknownにし、既存performance問題なのでcausal chainを`required`にする
- 具体計測 → actorのcheckout完了 → latency quality → 調査obligation → 再計測を往復traceする
- 原因調査と候補比較に必要なEvidence、反証条件、rejection criteriaを示す
- framing artifactのreadinessとarchitecture / implementationの未着手・blockを分ける

Forbidden:

- Redis、cache key、TTL、invalidation方式の選択
- 根拠のないDB、network、serialization等の原因確定
- architecture / contract / implementation成果物の作成

## C5 oracle

Required:

- debt findingのpriority assessmentがbusiness criticality、expected change、debt impact、failure risk、remediation costを一度ずつ持つ
- debt impactに出荷遅延・手動再受付・回帰の影響説明、Evidence、比較理由、人間owner recordを持ち、remediation costはunknownのまま扱う。ownerを特定できない入力では`status: unknown`、解決方法または未確定理由、Evidenceを残し、最終priorityを確定しない
- 注文品と配送品に別のstable term IDを付け、ambiguous termが両IDを参照し、actor / purpose / ruleをcanonical term recordへ保持する
- translationがcanonicalな`source_context_id`、`target_context_id`、`source_term_id`、`target_term_id`を個別に持ち、全IDがpackage内で解決する
- relationshipがcanonicalなstable ID、`upstream_context_id`、`downstream_context_id`、fact owner、integration、consistency、Evidence付きfailure ownerを持つ
- retry、duplicate、ambiguous outcomeをそれぞれ`required | not_applicable | unknown`で判定し、理由またはpolicy、owner、Evidence、unknownの`confirmation_method`と`impact_if_unresolved`を隠さない
- domain discoveryのreference integrityがchecked / unresolved IDとpass / failを明示し、未解決参照をfree textの意味一致でpassにしない
- transport redeliveryを業務上のretry可否またはduplicate resultの確定根拠にしない
- design artifactのreadinessと、未決policyに依存するimplementation blockerを分ける

Forbidden:

- 根拠のない数値priority score
- 一つの`商品`definitionまたはterm IDへの統合
- free textだけのambiguous meaning / translation
- retry、duplicate、recovery ownerの推測
- design modeでのrepository変更
