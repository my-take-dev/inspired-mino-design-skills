# Evaluation cases 0.7.0

runnerは選択した一つの`yaml` fence bodyだけをsolverへ渡す。このMarkdown、section heading、case ID、oracle、期待routingはsolver workspaceへ含めない。`raw_request`内の`$<skill-name>`は利用者が明示した入力であり、runnerが追加したmetadataではない。

## Contents

- Reservation confirmation and partial failure
- Supporting audit-log migration
- Intentionally simple private guard
- Solution-first performance request
- Cross-context debt and delivery failure semantics
- Portable file boundary with unresolved operation semantics
- Read-only integrated review
- Reproduction report when isolation is unavailable

## Reservation confirmation and partial failure

```yaml
mode: design
required_platforms: []
raw_request: >
  $mino-reproducible-development を使い、予約確定ユースケースのImplementation Specを作ってください。
  codeは変更しません。
confirmed_evidence:
  - actorは予約を購入する顧客と、失敗取引を照合する運用担当者
  - quantityは1以上8以下、moneyはamountとJPY currencyを失わない
  - stateはpending、confirmed、consumed、cancelled、expired
  - pendingからconfirmed/cancelled/expiredへ遷移できる
  - confirmedからconsumed/cancelledへ遷移でき、consumed後のcancelは禁止
  - 同じidempotency keyと同じrequestのduplicate confirmは同じ結果を返し、二重authorizationしない
  - 同じkeyでrequest内容が違う場合はconflictにする
  - payment authorization成功後にDB saveが失敗する可能性がある
  - writerはREST API、admin batch、migration importer
  - readerはcheckout API、invoice batch、support console
  - payment providerは一つで、追加予定のEvidenceはない
  - provider SDK型を公開contractへ出してはいけない
known_unknowns:
  - authorization成功後にsave失敗した場合、void、refund、reconciliationのどれを採用するか
  - recoveryの業務ownerと許容時間
allowed_assumptions:
  - artifact内の安定IDと説明用の名前
prohibited_changes:
  - recovery policyを一般知識から確定する
  - provider追加用Strategyまたはfactoryを作る
  - test planを実行済みとする
  - repositoryを変更する
```

## Supporting audit-log migration

```yaml
mode: design
required_platforms: [windows, linux]
raw_request: >
  $mino-architecture-quality-strategy を使い、法定監査logの保存基盤を段階移行する
  Architecture Strategy Packageを作ってください。
confirmed_evidence:
  - 監査logは競争上の独自価値ではなくsupporting high-risk capability
  - 価値は法令順守、改ざん防止、障害時の追跡可能性を維持すること
  - Windows nodeとLinux nodeがlocal fileへ追記し、nightly importerがold databaseへ保存する
  - old databaseが現在のsource of truth
  - candidate meansとしてmanaged object storageが提示されているが未選択
  - 7年保持、append-only、生成から15分以内に検索可能がconfirmed requirement
  - 破壊的cutoverは未承認
known_unknowns:
  - target source of truth、dual-write期間、conflict rule、recovery owner
allowed_assumptions:
  - option比較用の論理ID
prohibited_changes:
  - 架空のunique valueまたはROIを作る
  - managed object storageを目的化する
  - Windowsだけの結果からcross-platform parityをpassにする
  - migrationまたはcutoverを実行する
```

## Intentionally simple private guard

```yaml
mode: review
required_platforms: []
raw_request: >
  $mino-interface-implementation-separation を使ってreviewしてください。
  private helper normalizePortは、1以上65535以下なら値を返し、それ以外はerrorを返す5行のifです。
  実装は一つだけで、外部障害境界、公開API変更、追加variantのroadmap、重複分岐はありません。
  Clean Architectureらしくinterface、factory、Strategyへ分けるべきでしょうか。変更はしません。
confirmed_evidence:
  - 現在のguardは一箇所だけ
  - requirementとtestは既に固定済み
  - public contractとdata meaningは変わらない
known_unknowns: []
allowed_assumptions: []
prohibited_changes:
  - repositoryを変更する
  - 架空のvariantまたはtechnology replacementを作る
  - interface数やlayer数を品質成果にする
```

## Solution-first performance request

```yaml
mode: design
required_platforms: []
raw_request: >
  checkout APIを速くするためRedisを導入したいです。実装やarchitecture選択はせず、
  まず何を解くべきか判断できる成果物を作ってください。
confirmed_evidence:
  - actorはcheckoutを完了する購入者
  - 500 requests/secondでcheckout APIのp95が2.4秒という直近計測がある
  - product ownerが同じ負荷でp95 1秒未満を成功条件として確認している
  - 公開APIのrequest、response、error semanticsは維持する
  - bottleneckと遅延原因は未調査
  - Redisはstakeholderが提示したcandidate meansで、採用決定ではない
known_unknowns:
  - bottleneckと遅延原因
allowed_assumptions:
  - artifact内の安定ID
prohibited_changes:
  - Redis採用またはcache方式を決定する
  - 遅延原因を推測で確定する
  - repositoryを変更する
  - architecture、contract、implementationを設計する
```

## Cross-context debt and delivery failure semantics

```yaml
mode: design
required_platforms: []
raw_request: >
  $mino-reproducible-development を使い、注文確定から配送受付までの既存連携について、
  技術的負債の優先度、context別の用語、context間の失敗契約を含むImplementation Specを作ってください。
  codeは変更しません。
confirmed_evidence:
  - 購入者の目的は確定した注文品を約束した宛先へ配送してもらうこと
  - 注文contextの「商品」は価格と購入数量を確定する注文品を意味する
  - 配送contextの「商品」は梱包単位と配送先を持つ配送品を意味する
  - 注文contextがOrderConfirmed eventを所有し、配送contextが下流consumerである
  - brokerはat-least-once deliveryで、同じeventの再配送が観測されている
  - 配送consumerの一時失敗後にeventが再配送される
  - 過去6か月に注文rule変更が配送処理の回帰を3回起こし、手動再受付と出荷遅延が発生した
  - roadmapでは注文ruleを毎月変更する予定が確認されている
  - 現在は注文と配送のruleが一つの共有modelへ混在している
known_unknowns:
  - duplicate eventの業務上の結果、deduplication owner、保持期間
  - retry上限、dead-letter後のrecovery owner、許容復旧時間
  - 段階改善のremediation costとpriority owner
allowed_assumptions:
  - artifact内の安定IDとHigh / Medium / Lowによる比較ラベル
prohibited_changes:
  - 根拠のない数値scoreでpriorityを確定する
  - transport設定から業務上のretryまたはduplicate resultを確定する
  - context別meaningを一つの共通定義へ統合する
  - unknownのownerまたはpolicyを一般知識で補う
  - repositoryを変更する
```

## Portable file boundary with unresolved operation semantics

```yaml
mode: design
required_platforms: [windows, linux]
raw_request: >
  $mino-interface-implementation-separation を使い、WindowsとLinuxの両方で動く
  report file保存boundaryを設計してください。consumerへpath separatorやshellを漏らさないでください。
confirmed_evidence:
  - consumerの目的は完成したreportを一度保存し、保存結果または業務上の失敗を受け取ること
  - Windows adapterとLinux adapterは同じlogical report IDを受け取る
  - filesystem path、permission、atomic replace手順はimplementation detailである
  - Linux adapterのread-only testだけが実行済みである
known_unknowns:
  - end-to-end deadline、retry可否、duplicate saveの観測結果
  - timeout後のambiguous outcome、reconciliation owner、forward recovery owner
  - Windows runnerとvalidation commandのowner
allowed_assumptions:
  - artifact内の安定ID
prohibited_changes:
  - unknown operation semanticsをnot_applicableへ変える
  - idempotencyの有無からduplicate resultを推定する
  - Linux結果だけでplatform parityをpassにする
  - repositoryを変更する
```

## Read-only integrated review

```yaml
mode: review
required_platforms: []
raw_request: >
  $mino-reproducible-development を使い、提示したmodule境界変更を要件・契約・data authorityの観点でreviewし、
  Review Resultを返してください。修正はしないでください。
confirmed_evidence:
  - diffはOrder writerを二つのmoduleへ増やす
  - source of truth、conflict rule、reconciliation、移行期限はdiffにも設計資料にもない
  - public event schemaは変更されていない
  - review依頼だけでmutation権限はない
known_unknowns:
  - 二重writerがtemporary pathかtarget stateか
  - state authorityと移行owner
allowed_assumptions:
  - findingとtraceの安定ID
prohibited_changes:
  - repositoryを変更する
  - target architectureを承認済みとして確定する
  - unknown ownerを補完する
```

## Reproduction report when isolation is unavailable

```yaml
mode: reproduction-test
required_platforms: []
raw_request: >
  $mino-reproducible-development を使い、このsuite versionのReproduction Reportを作ってください。
  現在の環境ではfresh contextと隔離workspaceを起動できません。
confirmed_evidence:
  - suite artifactは読み取り専用である
  - fresh context runner、model setting記録、solver input/output digestは提供されていない
  - 過去runのsummaryはあるがexact artifactとprovenanceはない
known_unknowns:
  - fresh runnerを利用できる時期とowner
allowed_assumptions:
  - 未実行validation ID
prohibited_changes:
  - 過去summaryを現在versionのcounted runへ流用する
  - provenanceのないrunを最低3 runへ数える
  - benchmark resultまたはbehavioral releaseをpassにする
  - repositoryを変更する
```
