interface:
  display_name: "Interface / Implementation Separation"
  short_description: "consumer契約と内部技術を分離し、意味の隠蔽と過剰抽象化を防ぐ"
  default_prompt: "$mino-interface-implementation-separationで、依頼範囲の判断・根拠・未決事項と検証結果を返す。"
policy:
  allow_implicit_invocation: true
